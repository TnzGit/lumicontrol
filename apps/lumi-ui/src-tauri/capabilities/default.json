{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability set for the local LumiControl UI",
  "windows": ["main"],
  "permissions": ["core:default"]
}
